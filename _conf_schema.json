{
  "enabled": {
    "type": "bool",
    "description": "启用 Minecraft 控制台插件",
    "default": true
  },
  "admins": {
    "type": "list",
    "description": "允许使用 /mc-command 的管理员列表",
    "default": [111, 222, 333]
  },
  "rcon_host": {
    "type": "string",
    "description": "桥接插件监听地址（沿用旧字段名）",
    "default": "127.0.0.1"
  },
  "rcon_port": {
    "type": "int",
    "description": "桥接插件监听端口（沿用旧字段名）",
    "default": 25580
  },
  "rcon_password": {
    "type": "string",
    "description": "桥接插件鉴权令牌 token（沿用旧字段名）",
    "hint": "需要与 Minecraft 侧 bridge.token 保持一致",
    "default": ""
  },
  "timeout": {
    "type": "float",
    "description": "网络超时时间(秒)",
    "default": 5
  },
  "max_attempts": {
    "type": "int",
    "description": "网络层最多尝试次数（不会因为空输出重复执行命令）",
    "default": 2
  },
  "test_on_first_use": {
    "type": "bool",
    "description": "首次使用前是否先做一次 ping 测试",
    "default": true
  },
  "default_wait_ms": {
    "type": "int",
    "description": "未显式传 --t 时，默认等待日志的毫秒数",
    "default": 300
  },
  "max_output": {
    "type": "int",
    "description": "聊天回显最大长度（超过则截断）",
    "default": 1500
  }
}
